export declare const sce: {
    generateVideo(params: {
        prompt: string;
    }): Promise<string[]>;
};
export declare const suno: {
    generateAudio(params: {
        text: string;
    }): Promise<string>;
};
